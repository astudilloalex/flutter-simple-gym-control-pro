name: simple_gym_control_pro
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"
  
dependencies:
  bcrypt: ^1.1.3
  flutter:
    sdk: flutter
  get: ^4.6.1
  image_picker: ^0.8.5
  path: ^1.8.0
  percent_indicator: ^4.0.0
  responsive_design: ^0.1.5
  sqflite: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.8.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/db/
