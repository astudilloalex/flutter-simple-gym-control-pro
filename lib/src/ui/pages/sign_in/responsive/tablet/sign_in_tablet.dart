import 'package:flutter/material.dart';

class SignInTablet extends StatelessWidget {
  const SignInTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
